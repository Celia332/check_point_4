<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('home_index') }}">HOME</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        {% if is_granted('ROLE_USER') %}
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdown" role="button"
                       data-bs-toggle="dropdown" aria-expanded="false">
                        Les actualités sneakers
                    </a>
                    <ul class="dropdown-menu bg-dark" aria-labelledby="navbarDropdown">
                        <li class="dropdown-menu bg-dark" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item text-white" href="{{ path('sneakers') }}">Les nouveautés</a></li>
                    </ul>
                <li><a class="dropdown-item text-white py-2" href="{{ path('admin_index') }}">Administration</a></li>
                <li><a class="dropdown-item nav-link" href="{{ path('app_logout') }}">Se déconnecter</a></li>
                    <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                    <button class="btn btn-outline-success text-white border border-white" type="submit">Search</button>

                {% else %}
                <li><a class="dropdown-item text-white" href="{{ path('login') }}">Se connecter</a></li>
            </ul>
        </div>
    </div>
    {% endif %}
</nav>